<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowContentTransitions">true</item>
    </style>

    <style name="ListItem">
        <item name="android:layout_marginStart">@dimen/margin_list</item>
        <item name="android:layout_marginEnd">@dimen/margin_double</item>
    </style>

    <style name="ListItem.Title" parent="ListItem">
        <item name="android:textSize">@dimen/title_text_size</item>
        <item name="android:maxLines">1</item>
    </style>

    <style name="ListItem.Label" parent="ListItem">
        <item name="android:textSize">@dimen/label_text_size</item>
        <item name="android:maxLines">2</item>
    </style>

</resources>
